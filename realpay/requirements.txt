# 아래 버전은 Python 3.9~3.11 기준으로 고정한 값이다.
# 설치가 실패하면(특히 Python 3.12 이상에서는 numpy 1.24.4 / pandas 2.0.3의
# 휠이 없어 소스 빌드로 넘어가다 실패한다) 이 파일의 `== 버전` 부분을 지우고
# 패키지 이름만 남긴 뒤 다시 설치하면 된다.
#
#   pip install pandas numpy scikit-learn lightgbm xgboost shap streamlit plotly anthropic
#
# 단, 버전을 풀고 설치한 경우 model/artifacts/ 에 저장된 학습 결과 수치와
# 재현 결과가 달라질 수 있다.

pandas == 2.0.3
numpy == 1.24.4
scikit-learn == 1.3.2
lightgbm == 4.6.0
xgboost == 2.1.4
shap == 0.44.1
streamlit == 1.40.1
plotly == 6.9.0
anthropic == 0.72.0
